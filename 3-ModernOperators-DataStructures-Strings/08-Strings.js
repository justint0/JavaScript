"use strict";

console.log(`----- Strings -----`);

let str = " Hello, World! ";

console.log(str.toUpperCase());

console.log(str.toLowerCase());

console.log(str.trim());

console.log(str.replace("Hello", "Hi"));

// Other methods include .startsWith, .includes, .endsWith, .split, .join, .padStart, .padEnd, .repeat
